### Что я бы уточнил

1. Может ли один переводчик работать и в будни, и в выходные? (Тип `both`)
2. Нужна ли история занятости или достаточно текущего флага?
3. Требуется ли авторизация для API-эндпоинтов?
4. Нужна ли пагинация в списке переводчиков?
5. Нужны ли CRUD-методы?
6. Использовать Optios API или Composition API?
7. Можно ли брать данные из view или же они должны браться из API?

### Задача:

> **Доработать CRM бюро переводов:**
>
> 1. Добавить в таблицу `translators` поле `work_schedule` (тип: `weekday` / `weekend` / `both`)
>    и поле `is_available` (boolean — переводчик сейчас свободен или занят).
>
> 2. Реализовать миграции для создания таблицы `translators` с минимальным набором полей:
>    `id`, `full_name`, `language_pair`, `work_schedule`, `is_available`.
>
> 3. Написать методы ActiveRecord-модели `Translator` для выборки переводчиков
>    по типу дня (будний / выходной).
>
> 4. Добавить простой API-модуль (`/api/translators/status`), который возвращает
>    строку «Список переводчиков готов» или «Нет свободных переводчиков».
>
> 5. Отобразить список переводчиков на фронтенде через Vue.js-компонент,
>    встроенный в Yii2-представление.
>
> 6. Снабдить проект Docker-конфигурацией для локального запуска.